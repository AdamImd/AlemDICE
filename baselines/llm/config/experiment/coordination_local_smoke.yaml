# @package _global_

experiment:
  profile: coordination_local_smoke
  requires_api_key: false
  difficulties: [easy]
  seeds: [9999]

eval:
  num_episodes:
    alem: 1
  num_workers: 1
  max_steps_per_episode: 30
  save_images: false
  debug: true
  generate_debriefs: false

WANDB_MODE: disabled
